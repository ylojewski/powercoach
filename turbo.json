{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": [
        "$TURBO_DEFAULT$",
        "pnpm-lock.yaml",
        "tsconfig.json",
        "tsconfig.*.json",
        "tsup.config.*",
        "vite.config.*",
        "!dist/**"
      ],
      "outputs": ["dist/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "@powercoach/db#dev": {
      "cache": false,
      "persistent": false
    },
    "@powercoach/api#dev": {
      "cache": false,
      "dependsOn": ["@powercoach/db#dev"],
      "persistent": true
    },
    "@powercoach/manager#dev": {
      "cache": false,
      "dependsOn": ["@powercoach/db#dev"],
      "persistent": true,
      "with": ["@powercoach/api#dev"]
    },
    "format": {
      "inputs": ["$TURBO_DEFAULT$", "pnpm-lock.yaml", ".prettierignore", "prettier.config.*"],
      "outputs": []
    },
    "format:write": {
      "inputs": ["$TURBO_DEFAULT$", "pnpm-lock.yaml", ".prettierignore", "prettier.config.*"],
      "outputs": []
    },
    "lint": {
      "inputs": ["$TURBO_DEFAULT$", "pnpm-lock.yaml", "eslint.config.*"],
      "outputs": []
    },
    "pre-commit": {
      "cache": false,
      "dependsOn": ["^pre-commit"]
    },
    "test": {
      "inputs": [
        "$TURBO_DEFAULT$",
        "pnpm-lock.yaml",
        "tsconfig.json",
        "tsconfig.*.json",
        "vitest.config.*",
        "!coverage/**"
      ],
      "outputs": ["coverage/**"]
    },
    "typecheck": {
      "inputs": ["$TURBO_DEFAULT$", "pnpm-lock.yaml", "tsconfig.json", "tsconfig.*.json"],
      "outputs": []
    }
  }
}
