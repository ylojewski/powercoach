export enum LogLevel {
  debug = 'debug',
  error = 'error',
  fatal = 'fatal',
  info = 'info',
  silent = 'silent',
  trace = 'trace',
  warn = 'warn'
}

export enum NodeEnv {
  development = 'development',
  production = 'production',
  test = 'test'
}
