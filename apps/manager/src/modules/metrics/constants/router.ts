import { type ModuleRouteObject } from '@/core'

import { MetricsNavigationPath } from './navigation'

export const metricsRoutes = [
  {
    path: MetricsNavigationPath.Index
  }
] satisfies ModuleRouteObject[]
