import { type ModuleRouteObject } from '@/core'

import { NotesNavigationPath } from './navigation'

export const notesRoutes = [
  {
    path: NotesNavigationPath.Index
  }
] satisfies ModuleRouteObject[]
